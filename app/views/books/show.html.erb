show

<div>
  <%= @book.get_image %>
  <p>投稿ユーザー画像<%= image_tag @book.user.get_profile_image(100,100) %></p>
  <p>user <%= image_tag 'no_image2' %></p>
  <p>title <%= @book.title %></p>
  <p>opinion <%= @book.body %></p>
  <% if @book.user == current_user %>
    <%= link_to "削除", book_path(@book), method: :delete %>
  <% end %>
</div>